# Linux filesystem

## Linux filesystem structure

The Linux filesystem is organized in a hierarchical structure, starting from the root directory (`/`). Below are some of the common directories found in a Linux filesystem: